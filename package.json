{
  "name": "docker-dummy-app",
  "version": "1.0.0",
  "description": "A dummy app to use when setting up AWS ECS",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prod": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.3"
  }
}
